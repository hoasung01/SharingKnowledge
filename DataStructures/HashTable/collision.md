```html
- Collision in hash table is two keys has been assigned to same slot.
  So the newest value will be overwrite the old one.

- Solution:
  the easiest way is using linked list at that slot.
```