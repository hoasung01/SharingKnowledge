module OOP
  module Principles
    class Encapsulation
      def define
        <<~define_strip
        Encapsulation is the process of bundling data and the code 
        that operates on data together as part of a single entity(class).
        This prevents unauthorized access to the private data within the entity.
      end
    end
  end
end